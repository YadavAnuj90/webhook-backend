apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhookos.fullname" . }}
  labels:
    {{- include "webhookos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webhookos.selectorLabels" . | nindent 4 }}
